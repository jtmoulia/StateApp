<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pocketknife.state" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:debuggable="true">

        <activity
            android:name="com.pocketknife.state.StateListActivity"
            android:label="@string/title_activity_state_list">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.pocketknife.state.StateDetailActivity"
            android:label="StateDetailActivity">
        </activity>

        <activity
            android:name="com.pocketknife.state.StateActivity"
            android:label="@string/title_activity_state" >
        </activity>

        <provider
            android:name="com.pocketknife.state.StateProvider"
            android:authorities="com.pocketknife.state.provider" />

    </application>

</manifest>
